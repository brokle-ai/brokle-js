/**
 * Tests for LangChain callback handler
 *
 * Note: Full integration tests require langchain to be installed.
 * These are placeholder tests to verify the package builds correctly.
 */

import { describe, it, expect } from 'vitest';

describe('BrokleLangChainCallback', () => {
  it('should have tests placeholder', () => {
    // Placeholder test to prevent "no tests found" error
    expect(true).toBe(true);
  });
});